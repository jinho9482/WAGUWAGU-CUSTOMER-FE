import React from "react";
import {
  View,
  Text,
  FlatList,
  ActivityIndicator,
  StyleSheet,
} from "react-native";
import { gql, useQuery } from "@apollo/client";

const ReviewScreen = () => {
  const name = "gfgfg";
  const date = "1";
  const text = "ffgfgfgfgf";

  return <View></View>;
};

export default ReviewScreen;
